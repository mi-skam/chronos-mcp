[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --cov=chronos_mcp
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    --cov-fail-under=75
    --tb=short
